[project]
name = "contributor-readiness-kit"
version = "0.1.0"
description = "Small Python utility for contributor readiness notes"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
